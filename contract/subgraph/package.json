{
  "name": "ethglobal-istanbul",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ethglobal-istanbul",
    "create-local": "graph create --node http://localhost:8020/ ethglobal-istanbul",
    "remove-local": "graph remove --node http://localhost:8020/ ethglobal-istanbul",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ethglobal-istanbul",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.61.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
